<h2>Artists</h2>
<div class="list">
  <% @artists.sort_by {|artist| artist.name}.each do |artist| %>
    <p>
      <a href="/artist/<%= artist.name %>"><%= artist.name %></a>
    </p>
  <% end %>
</div>
